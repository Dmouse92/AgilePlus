# Fleet Triage Report 2026-05-02

Fleet-wide serial audit of git repositories under `/Users/kooshapari/CodeProjects/Phenotype/repos/`.

- Total git repositories scanned: 51
- Cargo workspace checks run serially: 25
- npm build checks run serially: 20
- Total failures found: 4

| Repo | Check | Severity | Status | Error Summary | Suggested Fix |
|------|-------|----------|--------|---------------|---------------|
| HexaKit | `cargo check --workspace` | High | Failed | `src/lib.rs` imports `hash::{blake3_hash, content_id, sha256_hash, HashAlgorithm}`, but those items are missing from the `hash` module. `pub mod signatures;` also references a missing `src/signatures.rs` or `src/signatures/mod.rs`. | Restore/add the missing `hash` module API and `signatures` module file, then rerun `cargo check --workspace`. |
| PhenoDevOps | `cargo check --workspace` | High | Failed | Same module/API break as HexaKit: unresolved `hash::{blake3_hash, content_id, sha256_hash, HashAlgorithm}` imports and missing `signatures` module file. | Restore the missing `hash` functions/types and `signatures` module implementation in the crate layout. |
| agileplus-agents | `cargo check --workspace` | High | Failed | Workspace member `crates/agileplus-agent-service` inherits `tonic-build` from `workspace.dependencies`, but the workspace root does not define `workspace.dependencies.tonic-build`. | Add `tonic-build` to `[workspace.dependencies]` in the workspace `Cargo.toml`, or change the member crate to use an explicit dependency. |
| phenokits-landing | `npm run build --if-present` | High | Failed | Astro/Vite/esbuild build failed with `Unexpected "export"` at `src/lib/site.ts:13`. | Fix `src/lib/site.ts` so the source is valid for the configured ESM build pipeline, then rerun `npm run build --if-present`. |

## Extended Audit Findings

Below are the most actionable findings discovered by an extended fleet sweep (repositories not already covered in the 2026-05-02 triage). Each entry lists the repository, issue type, severity, and a suggested fix.

| # | Repo | Issue | Severity | Suggested Fix |
|---|------|-------|----------|---------------|
| 1 | HeliosLab | `cargo test` fails: `pheno-ffi-python` test aborts because `libpython3.13.dylib` is not found at runtime (dyld cannot load shared lib). All other HeliosLab crate tests pass (54 tests OK). | **High** | Install matching Python 3.13 shared dev library so dynamic linker can find it. Options: (a) `brew install python@3.13` and ensure `/opt/homebrew/lib/libpython3.13.dylib` exists, or (b) set `DYLD_FALLBACK_LIBRARY_PATH` / `LIBRARY_PATH` to the Homebrew Cellar location. Alternatively, gate the `pheno-ffi-python` tests behind a feature flag so they are skipped when the dylib is unavailable. Rerun `cargo test` after fixing. |
| 2 | KlipDot | One unit test fails: `tests::test_display_server_detection` — panics on assertion that display server must be X11, Wayland, or Unknown. Likely environment detection mismatch on modern macOS/headless env. 47/48 tests pass. | **High** | Run with `RUST_BACKTRACE=1` to see the exact value. Update `test_display_server_detection` to handle the current macOS display environment (e.g., add a valid variant or make the assertion conditional on env). Rerun `cargo test --lib`. |
| 3 | agentapi-plusplus | Working tree has extensive uncommitted deletions (20+ files deleted incl. AGENTS.md, CHANGELOG.md, README.md, LICENSE, MAINTAINERS.md) plus untracked Taskfile.yml on branch `chore/funding-agents-20260502`. Risk of losing upstream repo metadata. | **High** | Determine if deletions were intentional (e.g., upstream divergence cleanup). If accidental, restore from upstream: `git checkout upstream/main -- <path>` or `git checkout origin/main -- <path>`. If intentional, commit immediately with a clear message. Before pushing, reconcile with both remotes (origin + upstream `coder/agentapi`). Also note: preserve AGENTS/CLAUDE upstream preamble per fork-aware README policy. |
| 4 | heliosApp | 13 stale branches merged into `main`, including old CI pin-actions branches and a dependabot branch. Clutters branch namespace and worktree discovery. | **Medium** | Review each branch: `git branch --merged main | grep -v '^\*\|main\|master'`. Delete merged local branches with `git branch -d <name>`. Delete remote-tracking refs with `git push origin --delete <name>` only if no open PRs reference them. Archive any work-in-progress branches to `repos/heliosApp-wtrees/<topic>/` per worktree policy before deleting. |
| 5 | phenoShared | 7 stale branches including `chore/pin-actions`, `fix/cargo-descriptions`, `fix/reqwest-tls-feature-name`, `lockfile-retry`. Housekeeping needed. | **Medium** | Same stale-branch cleanup as heliosApp. Archive any branches with unmerged work into worktrees. Delete fully merged branches. Document cleanup in worklog. |
| 6 | PolicyStack | Dirty working tree with uncommitted edits to `pytest.ini`, `resolve.py`, `tests/test_performance.py`, `tests/test_policy_contract.py`, and untracked `scripts/__init__.py`. Tests may be in unknown state. | **Medium** | Run `pytest` locally to confirm test suite state. Commit intentional changes (with rationale referencing any related spec/work package). Revert accidental edits. Ensure full test pass before pushing. |
| 7 | Parpoura | Uncommitted modifications to `docs/guides/AGILE_WORKSTREAM.md` and `docs/guides/COPILOT_L3_AGENTS.md` plus `.gitignore` change. Docs location is correct (under `docs/guides/`). | **Medium** | Commit the changes with a message referencing the relevant AgilePlus work package. Run `agileplus validate-encoding --all --fix` to verify UTF-8. Run Vale/markdownlint on the guides. |
| 8 | helios-cli | Untracked documentation file `docs/SOTA-helios-cli.md`. Correct location under `docs/`, but file is uncommitted. | **Low** | Move to appropriate `docs/` subdirectory (likely `docs/research/` for SOTA analysis or `docs/guides/` if how-to). Run Vale/markdownlint. Commit with clear message. |
| 9 | helios-router | Modified `.gitignore` and new untracked `docs/research/SOTA-helios-router.md`. Minor housekeeping. | **Low** | Commit or stash `.gitignore` changes. Ensure docs file follows UTF-8 and style rules. Commit both in a single docs/formatting commit. |
| 10 | thegent | 4 stale branches present with no active work. Low-priority cleanup to reduce branch noise. | **Low** | Review and prune: `git branch --merged main | grep -v '^\*\|main\|master'`, then `git branch -d` merged branches. Document cleanup in worklog. |

### Summary Statistics

| Metric | Value |
|--------|-------|
| Total repositories scanned | 55 |
| Repos with missing CLAUDE.md | 0 |
| Repos with uncommitted changes | 7 |
| Repos with stale merged branches (>4) | 5 |
| Cargo test failures (new) | 2 |
| cargo-deny.yml coverage | 100% of Cargo repos (18/18) |
| Repos needing security/dependency review | 0 (all current) |

### Action Priority

1. **Immediate (build/test blockers)**: Fix HeliosLab Python dylib, fix KlipDot display test
2. **High (data loss risk)**: Commit or restore agentapi-plusplus deleted files
3. **Medium (housekeeping)**: Clean heliosApp/phenoShared stale branches, commit PolicyStack/Parpoura changes
4. **Low (docs/formatting)**: Commit untracked docs in helios-cli/helios-router, prune thegent stale branches

Below are the most actionable findings discovered by an extended fleet sweep (repositories not already covered in the 2026-05-02 triage). Each entry lists the repository, issue type, severity, and a suggested fix.

| # | Repo | Issue | Severity | Suggested Fix |
|---|------|-------|----------|---------------|
| 1 | HeliosLab | `cargo test` fails: `pheno-ffi-python` test aborts because `libpython3.13.dylib` is not found at runtime (dyld cannot load shared lib). All other HeliosLab crate tests pass (54 tests OK). | **High** | Install matching Python 3.13 shared dev library so dynamic linker can find it. Options: (a) `brew install python@3.13` and ensure `/opt/homebrew/lib/libpython3.13.dylib` exists, or (b) set `DYLD_FALLBACK_LIBRARY_PATH` / `LIBRARY_PATH` to the Homebrew Cellar location. Alternatively, gate the `pheno-ffi-python` tests behind a feature flag so they are skipped when the dylib is unavailable. Rerun `cargo test` after fixing. |
| 2 | KlipDot | One unit test fails: `tests::test_display_server_detection` — panics on assertion that display server must be X11, Wayland, or Unknown. Likely environment detection mismatch on modern macOS/headless env. 47/48 tests pass. | **High** | Run with `RUST_BACKTRACE=1` to see the exact value. Update `test_display_server_detection` to handle the current macOS display environment (e.g., add a valid variant or make the assertion conditional on env). Rerun `cargo test --lib`. |
| 3 | agentapi-plusplus | Working tree has extensive uncommitted deletions (20+ files deleted incl. AGENTS.md, CHANGELOG.md, README.md, LICENSE, MAINTAINERS.md) plus untracked Taskfile.yml on branch `chore/funding-agents-20260502`. Risk of losing upstream repo metadata. | **High** | Determine if deletions were intentional (e.g., upstream divergence cleanup). If accidental, restore from upstream: `git checkout upstream/main -- <path>` or `git checkout origin/main -- <path>`. If intentional, commit immediately with a clear message. Before pushing, reconcile with both remotes (origin + upstream `coder/agentapi`). Also note: preserve AGENTS/CLAUDE upstream preamble per fork-aware README policy. |
| 4 | heliosApp | 13 stale branches merged into `main`, including old CI pin-actions branches and a dependabot branch. Clutters branch namespace and worktree discovery. | **Medium** | Review each branch: `git branch --merged main | grep -v '^\*\|main\|master'`. Delete merged local branches with `git branch -d <name>`. Delete remote-tracking refs with `git push origin --delete <name>` only if no open PRs reference them. Archive any work-in-progress branches to `repos/heliosApp-wtrees/<topic>/` per worktree policy before deleting. |
| 5 | phenoShared | 7 stale branches including `chore/pin-actions`, `fix/cargo-descriptions`, `fix/reqwest-tls-feature-name`, `lockfile-retry`. Housekeeping needed. | **Medium** | Same stale-branch cleanup as heliosApp. Archive any branches with unmerged work into worktrees. Delete fully merged branches. Document cleanup in worklog. |
| 6 | PolicyStack | Dirty working tree with uncommitted edits to `pytest.ini`, `resolve.py`, `tests/test_performance.py`, `tests/test_policy_contract.py`, and untracked `scripts/__init__.py`. Tests may be in unknown state. | **Medium** | Run `pytest` locally to confirm test suite state. Commit intentional changes (with rationale referencing any related spec/work package). Revert accidental edits. Ensure full test pass before pushing. |
| 7 | Parpoura | Uncommitted modifications to `docs/guides/AGILE_WORKSTREAM.md` and `docs/guides/COPILOT_L3_AGENTS.md` plus `.gitignore` change. Docs location is correct (under `docs/guides/`). | **Medium** | Commit the changes with a message referencing the relevant AgilePlus work package. Run `agileplus validate-encoding --all --fix` to verify UTF-8. Run Vale/markdownlint on the guides. |
| 8 | helios-cli | Untracked documentation file `docs/SOTA-helios-cli.md`. Correct location under `docs/`, but file is uncommitted. | **Low** | Move to appropriate `docs/` subdirectory (likely `docs/research/` for SOTA analysis or `docs/guides/` if how-to). Run Vale/markdownlint. Commit with clear message. |
| 9 | helios-router | Modified `.gitignore` and new untracked `docs/research/SOTA-helios-router.md`. Minor housekeeping. | **Low** | Commit or stash `.gitignore` changes. Ensure docs file follows UTF-8 and style rules. Commit both in a single docs/formatting commit. |
| 10 | thegent | 4 stale branches present with no active work. Low-priority cleanup to reduce branch noise. | **Low** | Review and prune: `git branch --merged main | grep -v '^\*\|main\|master'`, then `git branch -d` merged branches. Document cleanup in worklog. |

### Summary Statistics

| Metric | Value |
|--------|-------|
| Total repositories scanned | 55 |
| Repos with missing CLAUDE.md | 0 |
| Repos with uncommitted changes | 7 |
| Repos with stale merged branches (>4) | 5 |
| Cargo test failures (new) | 2 |
| cargo-deny.yml coverage | 100% of Cargo repos (18/18) |
| Repos needing security/dependency review | 0 (all current) |

### Action Priority

1. **Immediate (build/test blockers)**: Fix HeliosLab Python dylib, fix KlipDot display test
2. **High (data loss risk)**: Commit or restore agentapi-plusplus deleted files
3. **Medium (housekeeping)**: Clean heliosApp/phenoShared stale branches, commit PolicyStack/Parpoura changes
4. **Low (docs/formatting)**: Commit untracked docs in helios-cli/helios-router, prune thegent stale branches

